
/*
import { gameState } from "./state.js";
import { setVisualLevel } from "../visual.js";

export function checkProgression() {
  checkVisualProgression();
}

function checkVisualProgression() {
  const points = gameState.resources.points;

  if (points >= 25 && gameState.progression.visualLevel < 1) {
    setVisualLevel(1);
  }
}

*/